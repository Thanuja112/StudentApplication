spring:
  application:
    name: springapp
  datasource:
    url: jdbc:mysql://localhost:3306/studentdb
    username: root
    password: "amma nanna"
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
